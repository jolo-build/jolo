{
  "name": "@jolo/access",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun scripts/assets.js && wrangler dev --config ../../deploy/access.wrangler.jsonc --env development --port 8788",
    "build": "bun scripts/assets.js && wrangler deploy --config ../../deploy/access.wrangler.jsonc --env '' --dry-run --outdir dist",
    "deploy": "bun scripts/check-deploy.js && bun scripts/assets.js && wrangler deploy --config ../../deploy/access.wrangler.jsonc --env ''",
    "db:local": "wrangler d1 migrations apply ACCESS_DB --local --config ../../deploy/access.wrangler.jsonc --env development",
    "test": "bun test test && node scripts/test-worker.js && node scripts/test-task-runtime.js",
    "test:browser": "bun scripts/test-browser.js"
  },
  "devDependencies": {
    "wrangler": "4.130.0"
  }
}
