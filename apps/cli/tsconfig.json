{
  "extends": "../../tsconfig.base.json",
  // Bun runs the CLI; the interactive client renders React through Ink rather than a DOM.
  "compilerOptions": { "types": ["bun"], "jsx": "react-jsx" },
  "include": ["src", "test"]
}
